apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-project.fullname" . }}-backend
  labels:
    app: {{ include "dclaw-project.name" . }}
    component: backend
spec:
  type: {{ .Values.service.backend.type }}
  ports:
    - port: {{ .Values.service.backend.port }}
      targetPort: 8100
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-project.name" . }}
    component: backend

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-project.fullname" . }}-frontend
  labels:
    app: {{ include "dclaw-project.name" . }}
    component: frontend
spec:
  type: {{ .Values.service.frontend.type }}
  ports:
    - port: {{ .Values.service.frontend.port }}
      targetPort: 3010
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-project.name" . }}
    component: frontend
